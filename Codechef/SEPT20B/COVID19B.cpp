#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define endl "\n"
#define fast                          \
    ios_base::sync_with_stdio(false); \
    cin.tie(0);                       \
    cout.tie(0);

int main()
{
    fast;
    int testCases = 0;
    cin >> testCases;
    while (testCases--)
    {
        int N = 0;
        cin >> N;
        int velocity[N] = {0};
        for (int i = 0; i < N; i++)
        {
            cin >> velocity[i];
        }

        cout << " " << endl;
    }

    return 0;
}